local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/DeltaGay/femboy/refs/heads/main/GardenSpawner.lua"))()

Spawner.Load() -- UIをロード（必要なら）

local pets = Spawner.GetPets() -- ペット一覧を取得
print("Supported Pets:", pets)

local seeds = Spawner.GetSeeds() -- シード一覧を取得
print("Supported Seeds:", seeds)

-- ペットをスポーン（名前、体重、年齢を指定）
Spawner.SpawnPet("Raccoon", 1, 2)

-- シードをスポーン
Spawner.SpawnSeed("Candy Blossom")

-- 卵をスポーン
Spawner.SpawnEgg("Night Egg")

-- 花を回転（エフェクト的な？）
Spawner.Spin("Sunflower")
